# TCD Default Configuration
# Temporal Concept Discovery for 1D time-series vibration data

model:
  path: "path/to/cnn1d_model.ckpt"
  name: "cnn1d_wide"

data:
  path: "path/to/data"
  num_channels: 3  # X, Y, Z accelerometer axes
  seq_length: 2000  # timesteps
  sample_rate: 400  # Hz (5 seconds of data)
  class_names: ["OK", "NOK"]

analysis:
  layers: ["conv1", "conv2", "conv3", "conv4"]  # Conv layer names for CRP (flat attributes)
  composite: "epsilon_plus"  # LRP composite rule (no canonizers needed - no BatchNorm)
  batch_size: 32

tcd:
  variant: "A"  # A: filterbank, B: temporal_descriptors, C: learned_clusters
  n_concepts: 4
  # Variant A: Frequency bands in Hz (physics-informed for fault detection)
  filterbank_bands: 
    - [0, 10]     # Normal operation signatures
    - [10, 50]    # Transition band
    - [50, 100]   # Mid-frequency
    - [100, 200]  # Fault signatures (~150Hz)
  # Variant C: GMM prototypes
  n_prototypes: 4
  top_k_samples: 6
  gmm_covariance: "full"
  gmm_n_init: 1
  gmm_max_iter: 10

intervention:
  method: "suppress"  # suppress, amplify, or ablate
  target_layer: "conv1"
  concept_indices: []  # leave empty to auto-select

output:
  results_dir: "results"
  plots_dir: "plots"
  save_heatmaps: true
  save_features: true
